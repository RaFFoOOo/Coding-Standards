═══════════════════════════════════════════════════════════════════════════════
RESUME — N-way template sync, RE-RUN pass
Written 2026-09-03 · from the Coding-Standards (hub) repo, NOT le-cementine
═══════════════════════════════════════════════════════════════════════════════

You are the Lead Assistant & Advisor. RaFFoOOo is the Tech Lead.

WHY THIS FILE IS HERE, AND NOT IN le-cementine
  A second Claude Code session was working le-cementine's Sprint 51 concurrently. The
  checkpoint was placed in this repo so it could not collide with that session's tree.
  It is force-added past this repo's local `.gitignore` (which is itself untracked and
  lists `__resume_prompt.txt`). DELETE IT once the re-run's PRs merge — a stale
  checkpoint read as current is worse than none.

WHAT HAPPENED ON 2026-09-02
  A full N-way sync ran across Coding-Standards (hub) + le-cementine + one-talent
  against the 2026-07-16 baseline. Three PRs are OPEN and UNMERGED:

    Coding-Standards  #54   chore/sync-standards-2026-09-02 -> main
    le-cementine      #858  chore/sync-standards-2026-09-02 -> main
    one-talent        #41   chore/sync-standards-2026-09-02 -> main

  RaFFoOOo then asked to RE-RUN the sync before merging any of them, because
  le-cementine's Sprint 51 kept changing the standards files after the sync ran.

───────────────────────────────────────────────────────────────────────────────
PRIORITY 0 — VERIFY BEFORE TOUCHING ANYTHING
───────────────────────────────────────────────────────────────────────────────

0.1  THE GO/NO-GO CONDITION. The re-run must NOT start until le-cementine's
     PR #857 (sprint/51-design-system-scales -> main) has MERGED.

       cd ~/Projects/le-cementine && env -u GITHUB_TOKEN gh pr view 857 --json state,mergedAt

     WHY THIS IS THE GATE, and not "the other session finished": measured 2026-09-03,
     the ONLY standards drift since the sync was `AGENTS.md +5 lines` (a new §0
     "No Fabrication" rule), and it lives on origin/sprint/51-design-system-scales,
     NOT on origin/main. origin/main was still d5132ef5 — the exact base of PR #858.
     Re-running against an unmerged main would re-sync identical content and miss the
     very changes the re-run exists to collect. Re-check the drift yourself:

       git -C ~/Projects/le-cementine fetch origin -q
       git -C ~/Projects/le-cementine diff --stat origin/main origin/sprint/51-design-system-scales \
         -- AGENTS.md .claude/rules .claude/skills

     If #857 is still open, STOP and tell RaFFoOOo. Do not sync from a sprint branch.

0.2  Confirm the three sync PRs are still open and their branches unchanged:
       for r in Coding-Standards le-cementine one-talent; do
         (cd ~/Projects/$r && env -u GITHUB_TOKEN gh pr list --state open --head chore/sync-standards-2026-09-02)
       done

0.3  Confirm CI is not stalled. le-cementine's `CI_RUNNER` must hold a REAL runner
     label. On 2026-09-02 it was set to the literal "github", which is not a label, so
     `runs-on` resolved to it and EVERY run in the repo sat queued. RaFFoOOo fixed it to
     `ubuntu-latest`. Runs queued while it was wrong are pinned to the dead label and
     need cancel+re-run — they never self-recover, because `runs-on` resolves at run
     creation. See memory `feedback_ci_runner_var_must_be_a_real_label`.
       cd ~/Projects/le-cementine && env -u GITHUB_TOKEN gh variable list | grep CI_RUNNER

───────────────────────────────────────────────────────────────────────────────
PRIORITY 1 — RE-RUN THE SYNC (the main task)
───────────────────────────────────────────────────────────────────────────────

Read `.agents/workflows/sync-templates.md` in THIS repo (the hub) and follow it. The
notes below are what the 2026-09-02 run learned the hard way; they are corrections to
that workflow, not a replacement for it.

1.1  BUILD ON THE EXISTING SYNC BRANCHES. Do not close the three PRs and start clean.
     Branch each worktree from `chore/sync-standards-2026-09-02`, not from `main`.
     TWO reasons, both concrete:
       (a) Those branches carry ~50 hand-genericization edits (sprint/PR attributions,
           "RaFFoOOo", project class names, a CatalogItem/CatalogConfig code example).
           Starting from main throws all of it away and it must be redone by hand.
       (b) The ledger baseline. Each branch's sync-history.json already holds a
           `mode: "SYNC"` entry dated 2026-09-02 with 34 concept digests. Branching from
           those branches makes THAT the baseline, so the re-run is a clean incremental
           diff of only what Sprint 51 changed. Branching from main resets the baseline
           to 2026-07-16 and re-derives everything.
     Then merge origin/main into each branch first, so the base is current.

1.2  ISOLATION. le-cementine may still have a session on it. Use git worktrees for all
     three repos, never `git checkout` in the main working trees:
       git -C ~/Projects/<repo> worktree add <path> chore/sync-standards-2026-09-02
     After that, file tools need ABSOLUTE worktree paths — only Bash tracks cwd.
     (memory: feedback_worktree_absolute_paths)

1.3  THE MODEL THAT GOVERNS FAN-OUT — this is the one thing that is easy to get
     backwards, and the 2026-09-02 run DID get it backwards before catching it.
       The hub holds the GENERICIZED projection. Each spoke holds its CONCRETE
       instantiation. They are deliberately different documents for the same concept
       and this sync has preserved that for many cycles.
     Evidence, re-derivable in one command: `rule:naming-azure-resources` is byte-stable
     across every repo yet the hub's copy and le-cementine's differ by 63 lines — the hub
     has project-defined placeholders, le-cementine has its real resource map.
     CONSEQUENCE: le-cementine is the SOURCE. Never fan genericized text back into it.
     Its PR should be bookkeeping only (AGENTS.md, CLAUDE.md, sync-state, sync-history).
     one-talent DOES hold the generic version, so it is a real fan-out target.

1.4  AGENTS.md IS NOT IN THE CONCEPT REGISTRY and must be merged, never copied. It is
     now tracked as `doc:AGENTS`. The 2026-09-02 run merged all three copies; the
     re-run must fold in Sprint 51's new §0 "No Fabrication" rule:
       - le-cementine keeps it CONCRETE (it cites Sprint 51 T13, the "AMICO" pill,
         "Dettagli"/"Prenota" — real incident detail that is valuable in that repo).
       - The hub and one-talent get a GENERICIZED version of it.
     Verify afterwards that all three still contain "Measured Recommendations",
     "naming-azure-resources" and "Hub Completeness", and that NONE contains
     QUALITY_ASSURANCE / ARTIFACT_MANAGER / SPRINT_MANAGER (dead skill names the hub
     still carried until this sync removed them).

1.5  THE `github:*` FREEZE MUST BE CARRIED FORWARD. The 2026-09-02 entry deliberately
     froze all 14 `github:*` digests at their 2026-07-16 values for EVERY participant.
     That is not laziness — recomputing them would record the deferral as convergence
     and the gap would vanish silently. Keep them frozen in the new entry too, unless
     one-talent's CI has actually been adapted first (see PRIORITY 2).

1.6  `.github` IS ADAPTED PER REPO, NOT COPIED. Measured: of the 12 changed templates,
     10 were historically verbatim but `validate-archive.yml` (35 differing lines) and
     `ci-backend.yml` (19) were ADAPTED for the hub. Copying those two verbatim broke
     the hub's own CI in 4 seconds:
       bash: scripts/ci/check-workflow-cost-guards.sh: No such file or directory (127)
     The hub has 1 of the 14 guard scripts that workflow now invokes — the same position
     one-talent is in. The premise "the hub is a template repo, nothing executes there"
     was never checked and was false. Preserve the hub's adapted versions; port only
     genuinely portable mechanisms into them (the `concurrency` group and the
     `vars.CI_RUNNER` chain, per stack-github-actions.md §7.3 / §8.1).

1.7  GATES that must pass before opening/updating any PR:
       - Agnostic review gate, run ONCE on the hub tree, over AGENTS.md + .agents/.
         Note `git diff --name-only` EXCLUDES untracked files, so a newly added
         workflow (this is how `ux-review.md` nearly slipped through) must be gated
         separately.
       - Hub Completeness: every registry concept resolves to a real file in the hub.
         Was 34/34, 0 violations, `skipList` empty.
       - Hub shim consistency (Step 6c), must print nothing:
           cd <hub> && diff <( { ls .agents/skills; ls .agents/workflows | sed 's/\.md$//'; } | sort -u) <(ls .claude/skills | sort)
       - CLAUDE.md skill tables: 14 rows in all three. le-cementine's CLAUDE.md is
         ~414 lines of LIVE SPRINT STATE — PATCH the one table row, NEVER regenerate it,
         despite what sync-templates Step 5a says about CLAUDE.md always being generated.
       - le-cementine's main working tree untouched:
           git -C ~/Projects/le-cementine status --porcelain

───────────────────────────────────────────────────────────────────────────────
PRIORITY 2 — DEFERRED, ONLY IF RaFFoOOo ASKS
───────────────────────────────────────────────────────────────────────────────

one-talent's 12 `.github` CI templates were deliberately NOT synced. Its pipelines are
live (DISABLE_PIPELINES_FOR_TEMPLATE=false) and it has 1 of the 14 `scripts/ci/*.sh`
guards those templates invoke, plus no `tsconfig.e2e.json` / `npm run smoke:walk` for
the smoke-walk job. Full per-guard port-or-drop analysis is already written up in
`~/Projects/one-talent/TODO.md` (the entry added 2026-09-02). Do not start this
without an explicit ask.

───────────────────────────────────────────────────────────────────────────────
MERGE ORDER — enforce this
───────────────────────────────────────────────────────────────────────────────

  1. le-cementine #857 (sprint/51 -> main).  THE GATE. Nothing below starts first.
  2. Coding-Standards #53 and one-talent #39 — Dependabot `actions` group bumps.
     They touch .github/workflows/*. A manifest is a whole-file snapshot taken when the
     branch was cut (AGENTS.md §7), so merging either AFTER a sync PR silently reverts
     the sync's workflow changes. Merge them FIRST, or re-run Dependabot on them after.
  3. The three re-run sync PRs (#54, #858, #41), in any order — they are independent.

  NO PR MERGES WITHOUT RaFFoOOo'S EXPLICIT GO-AHEAD. This is a standing gate.

───────────────────────────────────────────────────────────────────────────────
OPERATOR ACTIONS (RaFFoOOo, not the agent)
───────────────────────────────────────────────────────────────────────────────

  - Cancel + re-run any le-cementine workflow run left queued from the CI_RUNNER
    incident. They cannot recover on their own.
  - Decide whether one-talent's CI adaptation (PRIORITY 2) is in scope.

───────────────────────────────────────────────────────────────────────────────
OPEN CONCERN TO RAISE WITH RaFFoOOo — not yet decided
───────────────────────────────────────────────────────────────────────────────

le-cementine's `.github/workflows/validate-archive.yml` `guards` job has NO
`if: vars.DISABLE_PIPELINES_FOR_TEMPLATE != 'true'` gate. `stack-github-actions.md §3`
requires that gate by name for the hygiene guards. This is why that job EXECUTED on the
hub, where the flag is `true`, and produced the exit-127 failure in 1.6 above. Harmless
in le-cementine itself (its flag is `false`), but it will bite the next repo scaffolded
from the template. Left unfixed on purpose — out of scope for a sync PR (memory:
feedback_scope_tight_prs). Needs its own small PR in le-cementine.

───────────────────────────────────────────────────────────────────────────────
READING LIST — read these before touching anything
───────────────────────────────────────────────────────────────────────────────

  ~/Projects/Coding-Standards/AGENTS.md
  ~/Projects/Coding-Standards/.agents/workflows/sync-templates.md   (the workflow)
  ~/Projects/le-cementine/CLAUDE.md                                 (active sprint pointer)
  ~/Projects/le-cementine/AGENTS.md                                 (post-#857 state)
  ~/Projects/one-talent/TODO.md                                     (the deferred CI entry)
  Memory: project_coding_standards_sync_pending,
          feedback_ci_runner_var_must_be_a_real_label,
          feedback_worktree_absolute_paths,
          feedback_scope_tight_prs

═══════════════════════════════════════════════════════════════════════════════
Run /resume-session to execute this checkpoint.
═══════════════════════════════════════════════════════════════════════════════
